# TEMPORARY: Dependabot version updates disabled to reduce Actions noise.
# Previous monthly npm + github-actions config is in git history (dependabot.yml before this change).
# Restore that file and remove dependabot[bot] skips from ci.yml / gitleaks.yml when re-enabling.

version: 2
updates: []
