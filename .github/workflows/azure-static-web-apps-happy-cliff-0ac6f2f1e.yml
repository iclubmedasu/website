name: Azure Static Web Apps CI/CD (Archived)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow notice
        run: |
          echo "This workflow is archived. members-portal now deploys as a container to Azure App Service via .github/workflows/deploy.yml"
