name: Deploy — Supabase + Hugging Face + Netlify

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  # ─────────────────────────────────────────────────────────────
  # JOB 1 — Run Prisma migrations against Supabase
  # ─────────────────────────────────────────────────────────────
  migrate:
    name: Run Database Migrations
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"

      - name: Install backend dependencies
        run: pnpm install --frozen-lockfile --filter backend...

      - name: Generate Prisma client
        run: pnpm --filter backend exec prisma generate

      - name: Run Prisma migrations
        working-directory: backend
        run: npx prisma migrate deploy
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

  # ─────────────────────────────────────────────────────────────
  # JOB 2 — Upload to Hugging Face Space
  # Uses huggingface_hub with Xet support — handles binary files
  # correctly and skips all frontend/asset files automatically.
  #
  # Required GitHub secrets:
  #   HF_TOKEN  — Hugging Face User Access Token (write access)
  #   HF_SPACE  — Space path e.g. "your-username/iclub-backend"
  # ─────────────────────────────────────────────────────────────
  deploy-backend:
    name: Deploy Backend to Hugging Face
    runs-on: ubuntu-latest
    needs: migrate

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Clean workspace before upload
        run: |
          # Remove frontend packages — not needed by HF
          # NOTE: packages/shared is kept — Dockerfile depends on it
          rm -rf members-portal public-website

          # Remove all node_modules and cache folders
          find . -type d -name "node_modules" -exec rm -rf {} + 2>/dev/null || true
          find . -type d -name ".cache" -exec rm -rf {} + 2>/dev/null || true
          find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true

          # Remove binary assets — backend doesn't need them
          find . -type f \( \
            -name "*.png" -o -name "*.jpg" -o -name "*.jpeg" \
            -o -name "*.gif" -o -name "*.ico" -o -name "*.webp" \
            -o -name "*.pdf" \
          \) -delete

          # Remove test and build output
          find . -type d -name "dist" -exec rm -rf {} + 2>/dev/null || true
          find . -type d -name "coverage" -exec rm -rf {} + 2>/dev/null || true
          find . -type d -name "playwright-report" -exec rm -rf {} + 2>/dev/null || true
          find . -type d -name "test-results" -exec rm -rf {} + 2>/dev/null || true

          echo "Workspace cleaned. Remaining files:"
          find . -not -path "./.git/*" -type f | head -50

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install huggingface_hub with Xet support
        run: pip install "huggingface_hub[hf_xet]"

      - name: Upload to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          HF_SPACE: ${{ secrets.HF_SPACE }}
        run: |
          python - <<'EOF'
          import os
          from huggingface_hub import HfApi

          api = HfApi(token=os.environ["HF_TOKEN"])
          repo_id = os.environ["HF_SPACE"]

          # Ensure the Space exists (no-op if it already does)
          api.create_repo(
              repo_id=repo_id,
              repo_type="space",
              space_sdk="docker",
              exist_ok=True,
          )

          api.upload_folder(
              folder_path=".",
              repo_id=repo_id,
              repo_type="space",
              ignore_patterns=[
                  ".git",
                  ".github",
                  ".env",
                  ".env.*",
                  "*.log",
                  "*.tsbuildinfo",
              ],
              commit_message="Deploy from GitHub Actions",
          )
          print("Upload complete")
          EOF

  # ─────────────────────────────────────────────────────────────
  # JOB 3 — Deploy frontend to Netlify
  #
  # Required GitHub secrets:
  #   NETLIFY_AUTH_TOKEN — Netlify personal access token
  #   NETLIFY_SITE_ID    — found in Netlify site settings
  #   HF_SPACE_URL       — iclubmedasu-backend.hf.space
  # ─────────────────────────────────────────────────────────────
  # deploy-frontend:
  #   name: Deploy Frontend to Netlify
  #   runs-on: ubuntu-latest
  #   needs: migrate
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #
  #     - name: Setup pnpm
  #       uses: pnpm/action-setup@v3
  #       with:
  #         version: 10
  #
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: "20"
  #         cache: "pnpm"
  #
  #     - name: Install all dependencies
  #       run: pnpm install --frozen-lockfile
  #
  #
  #     - name: Build members portal
  #       run: pnpm --filter members-portal build
  #       env:
  #         NEXT_PUBLIC_API_URL: https://${{ secrets.HF_SPACE_URL }}/api
  #
  #     - name: Deploy to Netlify
  #       uses: nwtgck/actions-netlify@v3
  #       with:
  #         publish-dir: members-portal/out
  #         production-deploy: true
  #         github-token: ${{ secrets.GITHUB_TOKEN }}
  #         deploy-message: "Deploy from GitHub Actions — ${{ github.sha }}"
  #       env:
  #         NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
  #         NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
