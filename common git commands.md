git pull origin main 

git status          # Check what files changed

git add .           # Stage all changes (or git add <specific-file>)

git commit -m "Your descriptive message here"   # Commit with message

git push origin main  OR  git push origin main --force


